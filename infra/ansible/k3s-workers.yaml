---
- hosts: k3s_workers
  become: yes

  vars:
    k3s_url: "https://192.168.64.10:6443"
    k3s_token: "TOKEN_FROM_MASTER"

  tasks:
    - name: Join cluster
      shell: |
        curl -sfL https://get.k3s.io | \
        K3S_URL={{ k3s_url }} \
        K3S_TOKEN={{ k3s_token }} \
        sh -